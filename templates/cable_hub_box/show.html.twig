{% extends 'base.html.twig' %}

{% block title %}CableHubBox{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cable_hub_box.id }}</td>
            </tr>
            <tr>
                <th>Longueur</th>
                <td>{{ cable_hub_box.longueur }}</td>
            </tr>
            <tr>
                <th>DateInstallation</th>
                <td>{{ cable_hub_box.dateInstallation ? cable_hub_box.dateInstallation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cable_hub_box_index') }}">back to list</a>

    <a href="{{ path('app_cable_hub_box_edit', {'id': cable_hub_box.id}) }}">edit</a>

    {{ include('cable_hub_box/_delete_form.html.twig') }}
{% endblock %}
