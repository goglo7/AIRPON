{% extends 'base.html.twig' %}

{% block title %}ReceptionHub{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reception_hub.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reception_hub.date ? reception_hub.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reception_hub_index') }}">back to list</a>

    <a href="{{ path('app_reception_hub_edit', {'id': reception_hub.id}) }}">edit</a>

    {{ include('reception_hub/_delete_form.html.twig') }}
{% endblock %}
