{% extends 'base.html.twig' %}

{% block title %}PositionOlt{% endblock %}

{% block body %}
    <table class="table table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ position_olt.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ position_olt.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary mb-5" href="{{ path('app_position_olt_index') }}">Retour</a>

    <a class="btn btn-secondary mb-5" href="{{ path('app_position_olt_edit', {'id': position_olt.id}) }}">Modifier</a>

    {{ include('position_olt/_delete_form.html.twig') }}
{% endblock %}
