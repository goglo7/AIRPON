{% extends 'base.html.twig' %}

{% block title %}TypeDirection{% endblock %}

{% block body %}
    <table class="table table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_direction.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_direction.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary mb-5" href="{{ path('app_type_direction_index') }}">Retour</a>

    <a class="btn btn-secondary mb-5" href="{{ path('app_type_direction_edit', {'id': type_direction.id}) }}">Modifier</a>

    {{ include('type_direction/_delete_form.html.twig') }}
{% endblock %}
