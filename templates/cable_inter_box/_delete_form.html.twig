<form method="post" action="{{ path('app_cable_inter_box_delete', {'id': cable_inter_box.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cable_inter_box.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
