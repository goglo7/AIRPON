<form method="post" action="{{ path('app_olt_delete', {'id': olt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ olt.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
