{% extends 'base.html.twig' %}

{% block title %}Liste des ReceptionOlt{% endblock %}

{% block body %}
    {% for type, messages in app.flashes %}
        {% for message in messages %}
            {%if type == 'error'%} {% set type = 'danger' %} {%endif%}
            <div class="alert alert-{{ type }}">
                {{ message|raw }}
            </div>
        {% endfor %}
    {% endfor %}
    <div class="d-flex align-items-center justify-content-end my-2">
        <a href="{{ path('app_reception_olt_new') }}" class="btn btn-primary">Créer</a>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reception_olt in reception_olts %}
            <tr>
                <td>{{ reception_olt.id }}</td>
                <td>{{ reception_olt.date ? reception_olt.date|date('Y-m-d') : '' }}</td>
                <td>
                    <a class="me-2 btn btn-primary" href="{{ path('app_reception_olt_show', {'id': reception_olt.id}) }}">Afficher</a>
                    <a class="me-2 btn btn-primary" href="{{ path('app_reception_olt_edit', {'id': reception_olt.id}) }}">Modifier</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
