{% extends 'base.html.twig' %}

{% block title %}ReceptionOlt{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reception_olt.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reception_olt.date ? reception_olt.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reception_olt_index') }}">back to list</a>

    <a href="{{ path('app_reception_olt_edit', {'id': reception_olt.id}) }}">edit</a>

    {{ include('reception_olt/_delete_form.html.twig') }}
{% endblock %}
