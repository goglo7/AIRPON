<form method="post" action="{{ path('app_c_type_delete', {'id': c_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ c_type.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
