{% extends 'base.html.twig' %}

{% block title %}CType{% endblock %}

{% block body %}
    <table class="table table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ c_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ c_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary mb-5" href="{{ path('app_c_type_index') }}">Retour</a>

    <a class="btn btn-secondary mb-5" href="{{ path('app_c_type_edit', {'id': c_type.id}) }}">Modifier</a>

    {{ include('c_type/_delete_form.html.twig') }}
{% endblock %}
