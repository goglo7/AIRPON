{% extends 'base.html.twig' %}

{% block title %}TypeOlt{% endblock %}

{% block body %}
    <table class="table table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_olt.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_olt.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary mb-5" href="{{ path('app_type_direction_index') }}">Retour</a>

    <a class="btn btn-secondary mb-5"  href="{{ path('app_type_olt_edit', {'id': type_olt.id}) }}">Modifier</a>

    {{ include('type_olt/_delete_form.html.twig') }}
{% endblock %}
